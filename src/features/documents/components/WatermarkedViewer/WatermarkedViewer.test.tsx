import { render, screen } from '@testing-library/react';
import { WatermarkedViewer } from './WatermarkedViewer';

describe('WatermarkedViewer', () => {
  it('renders without crashing', () => {
    render(<WatermarkedViewer />);
    // TODO: Add test assertions
  });
});
