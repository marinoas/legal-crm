import { makeStyles } from '@mui/styles';
import { Theme } from '@mui/material/styles';

export const useWatermarkedViewerStyles = makeStyles((theme: Theme) => ({
  root: {
    // TODO: Add styles
  },
}));
