import { render, screen } from '@testing-library/react';
import { CourtCalendar } from './CourtCalendar';

describe('CourtCalendar', () => {
  it('renders without crashing', () => {
    render(<CourtCalendar />);
    // TODO: Add test assertions
  });
});
