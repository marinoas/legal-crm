import { makeStyles } from '@mui/styles';
import { Theme } from '@mui/material/styles';

export const useAppointmentCardStyles = makeStyles((theme: Theme) => ({
  root: {
    // TODO: Add styles
  },
}));
